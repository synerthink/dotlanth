# Sample DotVM Contract
# This is a placeholder dot file for testing deployment

contract HelloWorld {
    version: "1.0.0"
    author: "DotLanth Team"
    
    function main() {
        print("Hello from DotVM!")
        return 42
    }
    
    function greet(name: string) {
        print("Hello, " + name + "!")
    }
}

# Configuration
memory: 512MB
cpu: 0.5
timeout: 30s