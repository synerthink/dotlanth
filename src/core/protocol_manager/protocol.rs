/// Represents a protocol with a name and description.
#[derive(Clone, Debug)]
pub struct Protocol {
    /// The name of the protocol.
    pub name: String,
    /// A brief description of the protocol.
    pub description: String,
}
