[package]
name = "audit"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Audit event types and append-only logging API"

[lib]
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }
