[package]
name = "auth"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Identity, sessions, authorization interfaces"

[lib]
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }
