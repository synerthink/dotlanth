[package]
name = "object_model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Entity types, entities, relationships, observations, provenance"

[lib]
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }
