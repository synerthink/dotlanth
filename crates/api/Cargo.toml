[package]
name = "api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "HTTP routes, DTOs, request extraction, response mapping"

[lib]
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }
